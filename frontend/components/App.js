export default class App {
  #voyages = [];

  async init() {
    // init add voyage modal
    // event listener for add voyage button press
    try {
      // load and render voyages from Model
    } catch (err) {
      console.error(err);
    }
  }
}
